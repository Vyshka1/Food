/**
 * Цвета БЖУ живут в одном месте: они встречаются в кольце нормы, в легенде
 * онбординга, в профиле и в процентах на меню — расходиться им нельзя.
 */
export const MACRO_COLOR = {
  protein: '#8ec06c',
  fat: '#e0b352',
  carbs: '#3f7233',
} as const

/** Фон дорожки кольца — тот же, что у мягкой карточки в стилях. */
export const RING_TRACK = '#eae7d9'

/**
 * Цвет заглушки блюда — по его виду.
 *
 * Фотографий пока нет, и на неделе получалось двадцать восемь одинаковых
 * плиток: доска переставала что-либо показывать, оставаясь обоями. Цвет по виду
 * возвращает ей смысл — по неделе сразу видно, что она мясная или что одно и то
 * же блюдо стоит четыре раза.
 *
 * Заглушка нарочно не притворяется снимком: это плашка с иконкой, а не размытое
 * фото. Когда появятся настоящие фотографии, они встанут на её место сами —
 * каскад в lib/photos это уже умеет.
 */
export const KIND_TINT: Record<string, string> = {
  soup: '#e6efe1',
  porridge: '#f3ecdc',
  salad: '#e4f0df',
  drink: '#e8eef4',
  pancake: '#f5e9dc',
  bake: '#f0e6da',
  fish: '#e2edf1',
  meat: '#f2e3df',
  apple: '#eef1e2',
  pan: '#ebe9e0',
}

/** Цвет для вида, которого нет в таблице: нейтральный, как у сковороды. */
export const KIND_TINT_FALLBACK = KIND_TINT.pan
